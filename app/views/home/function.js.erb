$('#queue').empty();
$('#color').val('');
$('#color').focus();

<% session[:stack].data.each do |color| %>
  var colorbox = $('<div>');
  colorbox.addClass('box');
  colorbox.css('background-color', '<%= color %>');
  $('#queue').prepend(colorbox);
<% end %>
